// String, number, undefined, null, boolean, symbol
const nome = 'Pedro'; // String
const nome1 = "Antônio"; // String
const nome2 = `Josué`; // String

const num1 = 10; // Number
const num2 = 22.7; // Number

let nomeAluno; // Undefined = não aponta pra nenhum local na memória
let sobrenomeAluno = null; // Nulo -> não aponta pra nenhum local na memória

const aprovado = true; // Booleano = true, false (lógico)

let a = 2;
const b = a;
a = 3;
console.log(a, b);